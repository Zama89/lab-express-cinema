<div class="container">
    <a href="/">Home</a>
    <div id="film">
        {{#each allmovies}}
        <div class="card">
        <img src="{{image}}" alt="poster">
        <h2>{{title}}</h2>
        <button><a href="/movies/{{_id}}">More films</a></button>
        </div>
        {{/each}}
    </div>
</div>